/** A plain gap between metadata items, in place of a middle-dot separator. The space inside keeps words apart for screen readers. */
export function Sep() {
  return <span className="inline-block w-3">{" "}</span>;
}
